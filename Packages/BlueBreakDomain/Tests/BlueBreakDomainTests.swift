import Testing

@testable import BlueBreakDomain

/// Smoke test: the module exists, compiles and is importable. The `import` above
/// is the assertion; behaviour arrives with the slices that add behaviour.
@Test func domainModuleIsImportable() {}

//  BlueBreakDesignSystem
//
//  Colour and spacing tokens, typography, motion curves, shader-backed effects
//  and the accessibility fallbacks for them. SwiftUI is allowed; business modules
//  are not — this package depends on no other BlueBreak package, so it stays
//  reusable and independently previewable (architecture.md §8).

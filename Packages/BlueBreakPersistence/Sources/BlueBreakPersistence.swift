//  BlueBreakPersistence
//
//  SwiftData models, settings storage, repositories, checkpointing and
//  migrations. Implements the persistence ports declared in
//  BlueBreakApplication, which is its only BlueBreak dependency.
//
//  SwiftData lives here and nowhere else; SwiftUI is forbidden
//  (architecture.md §8).
